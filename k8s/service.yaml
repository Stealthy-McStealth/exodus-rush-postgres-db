apiVersion: v1
kind: Service
metadata:
  name: postgres-db
  namespace: passover
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: postgres-db
